export class CanvasLoad {
    sequenceIndex: number;
    canvasIndex: number;
    constructor(sequenceIndex: number, canvasIndex: number) {
        this.sequenceIndex = sequenceIndex;
        this.canvasIndex = canvasIndex;
    }
}