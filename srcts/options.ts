export interface Options {
    manifestUrl?: string,
    navigation?: {
        enabled?: boolean,
        opened?: boolean
    },
    annotations?: {
        enabled?: boolean,
        opened?: boolean
    }
}